import fs from 'node:fs';
import path from 'node:path';
import {spawnSync} from 'node:child_process';
const root=process.argv[2]||'.janu-live';
const dir=path.dirname(new URL(import.meta.url).pathname);
const files=fs.readdirSync(root).filter(f=>f.endsWith('.gs')||f.endsWith('.js'));
const target=files.find(f=>{const t=fs.readFileSync(path.join(root,f),'utf8');return t.includes('function runOwnedRendererCanaryTick(')&&t.includes('function rendererFreshCanaryEnqueue_(');});
if(!target)throw new Error('Owner canary target missing');
const file=path.join(root,target);let s=fs.readFileSync(file,'utf8');
function rangeOf(name){const start=s.indexOf('function '+name+'(');if(start<0)return null;const open=s.indexOf('{',start);let d=0,q=null,e=false;for(let i=open;i<s.length;i++){const c=s[i];if(q){if(e){e=false;continue;}if(c==='\\'){e=true;continue;}if(c===q)q=null;continue;}if(c==='"'||c==="'"||c==='`'){q=c;continue;}if(c==='{')d++;else if(c==='}'&&--d===0)return{start,end:i+1};}throw new Error('Unterminated '+name);}
function replaceFn(name,code){const r=rangeOf(name);if(!r)throw new Error(name+' missing');s=s.slice(0,r.start)+code+s.slice(r.end);}
function addBefore(anchor,token,code){if(s.includes(token))return;const i=s.indexOf(anchor);if(i<0)throw new Error('Missing anchor '+anchor);s=s.slice(0,i)+code+'\n'+s.slice(i);}

addBefore('function runOwnedRendererCanaryTick(','function rendererCanaryRepeatDecision_(',`function rendererCanaryRepeatDecision_(priorStatus,priorDeployment,currentDeployment){const st=String(priorStatus||'').toLowerCase(),p=String(priorDeployment||''),c=String(currentDeployment||'');if(st!=='failed')return 'ALLOW';if(p&&c&&p===c)return 'BLOCK_REPEAT_SAME_DEPLOYMENT';return 'ALLOW_CHANGED_OR_UNTRACKED_DEPLOYMENT';/* CANARY-REPEAT-GUARD-001 */}
function rendererCanaryFailureClass_(code,detail){const t=(String(code||'')+' '+String(detail||'')).toUpperCase();if(t.indexOf('RENDER_BULLET_LOSS')>=0)return 'RENDER_BULLET_LOSS';if(t.indexOf('INTERNAL_TAG_LEAK')>=0)return 'INTERNAL_TAG_LEAK';if(t.indexOf('DOCUMENTAPP')>=0&&t.indexOf('PERMISSION')>=0)return 'DOCUMENTAPP_AUTH';if(t.indexOf('CANARY_REPEAT_BLOCKED')>=0)return 'CANARY_REPEAT_BLOCKED';return String(code||'DETERMINISTIC_UNKNOWN').toUpperCase().replace(/[^A-Z0-9_]+/g,'_').slice(0,120)||'DETERMINISTIC_UNKNOWN';}
function rendererCanaryFailureFields_(qid){const q=SH_(JC.S.Q),m=hm_(q);for(let r=q.getLastRow();r>=2;r--){if(String(q.getRange(r,m['Queue Job ID']).getDisplayValue()||'')!==String(qid||''))continue;return{status:String(q.getRange(r,m['Status']).getDisplayValue()||'').toLowerCase(),code:m['Error Code']?String(q.getRange(r,m['Error Code']).getDisplayValue()||''):'',detail:m['Error Detail']?String(q.getRange(r,m['Error Detail']).getDisplayValue()||''):''};}return{status:'',code:'',detail:''};}
function rendererCanaryAttemptContext_(){const raw=rendererWorkerStateValue_('renderer_canary_last_attempt_context');if(!raw)return{};try{return JSON.parse(String(raw));}catch(e){return{};}}
function rendererCanaryDeployment_(){return String(rendererWorkerStateValue_('deployment_git_commit')||rendererWorkerStateValue_('deployment_source_sha256')||'');}
function rendererCanaryFailureSignature_(code,detail){return hash_([String(P12.VERSION||''),rendererCanaryFailureClass_(code,detail)].join('|'));}
function rendererCanaryRecordAttempt_(qid){if(!qid)return;const ctx={contract:'CANARY-REPEAT-GUARD-001',queueJobId:String(qid),deployment:rendererCanaryDeployment_(),recordedAt:typeof iso_==='function'?iso_():new Date().toISOString()};upsertWorkerState_('renderer_canary_last_attempt_context',JSON.stringify(ctx),JSON.stringify(ctx).slice(0,1500));}
function rendererCanaryPersistFailureFromQueue_(qid){const f=rendererCanaryFailureFields_(qid);if(f.status!=='failed')return null;const sig=rendererCanaryFailureSignature_(f.code,f.detail),ctx=rendererCanaryAttemptContext_(),evidence={contract:'CANARY-REPEAT-GUARD-001',queueJobId:String(qid||''),signature:sig,failureClass:rendererCanaryFailureClass_(f.code,f.detail),deployment:String(ctx.deployment||rendererCanaryDeployment_()),status:f.status,firstSeenAt:typeof iso_==='function'?iso_():new Date().toISOString()};upsertWorkerState_('renderer_canary_failure_guard','UNRESOLVED',JSON.stringify(evidence).slice(0,1500));return evidence;}
function rendererCanaryAdmissionGuard_(){const ctx=rendererCanaryAttemptContext_();if(!ctx.queueJobId)return{decision:'ALLOW_NO_PRIOR_ATTEMPT'};const f=rendererCanaryFailureFields_(ctx.queueJobId),cur=rendererCanaryDeployment_(),decision=rendererCanaryRepeatDecision_(f.status,ctx.deployment,cur);if(f.status==='succeeded'){upsertWorkerState_('renderer_canary_failure_guard','RESOLVED',JSON.stringify({contract:'CANARY-REPEAT-GUARD-001',queueJobId:ctx.queueJobId,deployment:ctx.deployment}).slice(0,1500));return{decision:'ALLOW_PRIOR_SUCCESS'};}if(f.status==='failed'){const sig=rendererCanaryFailureSignature_(f.code,f.detail),evidence={contract:'CANARY-REPEAT-GUARD-001',queueJobId:ctx.queueJobId,signature:sig,failureClass:rendererCanaryFailureClass_(f.code,f.detail),priorDeployment:String(ctx.deployment||''),currentDeployment:cur,decision:decision};upsertWorkerState_('renderer_canary_failure_guard','UNRESOLVED',JSON.stringify(evidence).slice(0,1500));if(decision==='BLOCK_REPEAT_SAME_DEPLOYMENT')throw new Error('DETERMINISTIC:CANARY_REPEAT_BLOCKED:'+sig+':'+evidence.failureClass);return{decision:decision};}return{decision:'ALLOW_PRIOR_NONFAILED'};}`);

if(rangeOf('ensureOwnerWorkerTrigger_'))replaceFn('ensureOwnerWorkerTrigger_',`function ensureOwnerWorkerTrigger_(){const canonical='phase1OneJobTick';let ts=ScriptApp.getProjectTriggers(),matches=ts.filter(t=>String(t.getHandlerFunction()||'')===canonical);for(let i=1;i<matches.length;i++)ScriptApp.deleteTrigger(matches[i]);if(matches.length===0)ScriptApp.newTrigger(canonical).timeBased().everyMinutes(5).create();const rb=ownerWorkerTriggerTopology_();if(rb.canonicalCount!==1)throw new Error('DETERMINISTIC:OWNER_WORKER_TRIGGER_TOPOLOGY_MISMATCH:'+JSON.stringify(rb));upsertWorkerState_('canonical_worker_trigger_status','PASS',JSON.stringify({contract:'CANONICAL-WORKER-TRIGGER-001',intervalMinutes:5,canonicalHandler:canonical,canonicalCount:rb.canonicalCount,legacyClosureCount:rb.legacyClosureCount,handlers:rb.handlers}).slice(0,1500));return rb;/* CANONICAL-WORKER-TRIGGER-001 / CANONICAL-WORKER-5MIN-001 */}`);

replaceFn('runOwnedRendererCanaryTick',`function runOwnedRendererCanaryTick(){const started=typeof iso_==='function'?iso_():new Date().toISOString();upsertWorkerState_('renderer_canary_trigger_last_start',started,'CONTROL-PLANE-CANARY-TELEMETRY-001 owner one-shot entered');let qid='';try{if(!controlPlaneExecutionAuthorized_())throw new Error('CONTROL_OWNER_MISMATCH');qid=rendererWorkerStateValue_('renderer_canary_pending_queue_id');let rb=qid?rendererCanaryQueueReadback_(qid):null;if(!rb||!rb.found||rb.status!=='queued'||rb.attempts!==0){rendererCanaryAdmissionGuard_();const fresh=rendererFreshCanaryEnqueue_();qid=fresh.queueJobId;upsertWorkerState_('renderer_canary_pending_queue_id',qid,JSON.stringify({contract:'CONTROL-PLANE-CANARY-FRESH-001',replacedStale:true}).slice(0,1500));rb=rendererCanaryQueueReadback_(qid);}if(!rb||rb.status!=='queued'||rb.attempts!==0)throw new Error('FRESH_CANARY_NOT_QUEUED:'+JSON.stringify(rb));rendererCanaryRecordAttempt_(qid);const out=rendererExactCanaryExecute_(qid);upsertWorkerState_('renderer_canary_failure_guard','RESOLVED',JSON.stringify({contract:'CANARY-REPEAT-GUARD-001',queueJobId:qid,deployment:rendererCanaryDeployment_()}).slice(0,1500));upsertWorkerState_('renderer_canary_trigger_last_result','PASS',JSON.stringify({contract:'CONTROL-PLANE-CANARY-TELEMETRY-001',queueJobId:qid,startedAt:started,finishedAt:typeof iso_==='function'?iso_():new Date().toISOString()}).slice(0,1500));return out;}catch(e){if(qid)rendererCanaryPersistFailureFromQueue_(qid);upsertWorkerState_('renderer_canary_trigger_last_result','FAIL',JSON.stringify({contract:'CONTROL-PLANE-CANARY-TELEMETRY-001',startedAt:started,finishedAt:typeof iso_==='function'?iso_():new Date().toISOString(),error:String((e&&e.stack)||e)}).slice(0,1500));throw e;}/* CONTROL-PLANE-CANARY-FRESH-001 / CONTROL-PLANE-CANARY-TELEMETRY-001 / CANARY-REPEAT-GUARD-001 */}`);
if(rangeOf('installOwnedRendererCanaryTrigger'))replaceFn('installOwnedRendererCanaryTrigger',`function installOwnedRendererCanaryTrigger(){if(!controlPlaneExecutionAuthorized_())throw new Error('CONTROL_OWNER_MISMATCH');for(const t of ScriptApp.getProjectTriggers())if(String(t.getHandlerFunction()||'')==='runOwnedRendererCanaryTick')ScriptApp.deleteTrigger(t);ScriptApp.newTrigger('runOwnedRendererCanaryTick').timeBased().after(60*1000).create();upsertWorkerState_('renderer_canary_trigger_installed_at',typeof iso_==='function'?iso_():new Date().toISOString(),'CONTROL-PLANE-CANARY-TELEMETRY-001 one-shot scheduled after >=60s');return{pass:true,contract:'CONTROL-PLANE-CANARY-ONLY-001',telemetry:'CONTROL-PLANE-CANARY-TELEMETRY-001'};}`);

const ciRange=rangeOf('runRendererAuthorizedCanaryEnqueue');if(ciRange){let fn=s.slice(ciRange.start,ciRange.end);if(!fn.includes('CANARY-REPEAT-GUARD-CI-001')){const exact="const pending=rendererPendingCanaryQueued_(),queued=pending||rendererFreshCanaryEnqueue_();";if(fn.includes(exact)){fn=fn.replace(exact,"const pending=rendererPendingCanaryQueued_();if(!pending)rendererCanaryAdmissionGuard_();const queued=pending||rendererFreshCanaryEnqueue_();");}else if(!fn.includes('rendererCanaryAdmissionGuard_()'))throw new Error('CI enqueue admission anchor missing');const anchor="upsertWorkerState_('renderer_canary_pending_queue_id'";const i=fn.indexOf(anchor);if(i<0)throw new Error('CI enqueue pointer anchor missing');fn=fn.slice(0,i)+"rendererCanaryRecordAttempt_(queued.queueJobId);/* CANARY-REPEAT-GUARD-CI-001 */"+fn.slice(i);s=s.slice(0,ciRange.start)+fn+s.slice(ciRange.end);}}

const ciGuardOk=!rangeOf('runRendererAuthorizedCanaryEnqueue')||s.includes('CANARY-REPEAT-GUARD-CI-001'),workerPolicyOk=!rangeOf('ensureOwnerWorkerTrigger_')||s.includes('CANONICAL-WORKER-5MIN-001');if(!s.includes('CONTROL-PLANE-CANARY-FRESH-001')||!s.includes('CONTROL-PLANE-CANARY-TELEMETRY-001')||!s.includes('CANARY-REPEAT-GUARD-001')||!ciGuardOk||!workerPolicyOk)throw new Error('canary recurrence/telemetry/worker marker missing');fs.writeFileSync(file,s);
const test=spawnSync(process.execPath,[path.resolve(dir,'test-pack-live-evidence-sanitization.mjs'),path.resolve(dir,'..')],{encoding:'utf8'});if(test.status!==0)throw new Error(test.stderr||test.stdout||'PACK-SAN-LIVE-METAFORMS-001 regression failed');
s=fs.readFileSync(file,'utf8');let packSanitization='fixture-only';
if(s.includes('function pack_(')&&s.includes('function stripInternalEvidenceTags_(')&&s.includes('function assertExternalTextClean_(')){
 const patch=spawnSync(process.execPath,[path.resolve(dir,'patch-pack-live-evidence-sanitization.mjs'),root],{encoding:'utf8'});if(patch.status!==0)throw new Error(patch.stderr||patch.stdout||'PACK-SAN-LIVE-EVIDENCE-001 patch failed');
 s=fs.readFileSync(file,'utf8');if(!s.includes('PACK-SAN-LIVE-EVIDENCE-001'))throw new Error('pack live-evidence sanitizer missing after convergence');packSanitization='PACK-SAN-LIVE-EVIDENCE-001';
}
const sourcingTest=spawnSync(process.execPath,[path.resolve(dir,'test-sourcing-scheduler-health.mjs'),path.resolve(dir,'..')],{encoding:'utf8'});if(sourcingTest.status!==0)throw new Error(sourcingTest.stderr||sourcingTest.stdout||'SOURCING-RECOVERY-NOT-LIVENESS-001 regression failed');
if(s.includes('function sourceFreshnessHealth_(')){
 const sourcingPatch=spawnSync(process.execPath,[path.resolve(dir,'patch-sourcing-scheduler-health.mjs'),root],{encoding:'utf8'});if(sourcingPatch.status!==0)throw new Error(patch.stderr||patch.stdout||'SOURCING-HEALTH-BLOCKER-PRECEDENCE-001 patch failed');
 s=fs.readFileSync(file,'utf8');if(!s.includes('SOURCING-HEALTH-BLOCKER-PRECEDENCE-001'))throw new Error('sourcing scheduler health contract missing after convergence');
}
const ck=spawnSync(process.execPath,['--check',file],{encoding:'utf8'});if(ck.status!==0)throw new Error(ck.stderr);console.log(JSON.stringify({status:'PASS',contract:'CONTROL-PLANE-CANARY-FRESH-001',canaryTelemetry:'CONTROL-PLANE-CANARY-TELEMETRY-001',canaryRepeatGuard:'CANARY-REPEAT-GUARD-001',canonicalWorkerMinutes:5,packSanitization:packSanitization,sourcingHealth:'SOURCING-HEALTH-BLOCKER-PRECEDENCE-001',file:target},null,2));