import fs from 'node:fs';
import path from 'node:path';
const root=process.argv[2]||'.janu-live';
const files=fs.readdirSync(root).filter(f=>f.endsWith('.gs')||f.endsWith('.js'));
const target=files.find(f=>{const t=fs.readFileSync(path.join(root,f),'utf8');return t.includes('function verifyReleaseIdentity()')&&t.includes('const P12');});
if(!target)throw new Error('TrackerWorkflow source not found');
const file=path.join(root,target);let s=fs.readFileSync(file,'utf8'),before=s;
const anchor='function verifyReleaseIdentity()';

function rangeOf(name){const sig='function '+name+'(';const start=s.indexOf(sig);if(start<0)return null;const open=s.indexOf('{',start);if(open<0)throw new Error('Malformed '+name);let depth=0,quote=null,esc=false;for(let i=open;i<s.length;i++){const c=s[i];if(quote){if(esc){esc=false;continue;}if(c==='\\'){esc=true;continue;}if(c===quote)quote=null;continue;}if(c==='"'||c==="'"||c==='`'){quote=c;continue;}if(c==='{')depth++;else if(c==='}'&&--depth===0)return{start,end:i+1};}throw new Error('Unterminated '+name);}
function add(token,code){if(s.includes(token))return;const i=s.indexOf(anchor);if(i<0)throw new Error('release identity anchor missing');s=s.slice(0,i)+code+'\n'+s.slice(i);}
function replaceFn(name,code){const r=rangeOf(name);if(r)s=s.slice(0,r.start)+code+'\n'+s.slice(r.end);else add('function '+name+'(',code);}
function append(name,marker,code){if(s.includes(marker))return;const r=rangeOf(name);if(!r)throw new Error(name+' missing');s=s.slice(0,r.end-1)+code+s.slice(r.end-1);}

for(const dep of ['function enqueue_(','function upsertWorkerState_(','function p1aPropagateClosedVacancy_('])if(!s.includes(dep))throw new Error('E2E continuation dependency missing '+dep);
if(!s.includes('RESUME_GENERATE'))throw new Error('RESUME_GENERATE worker contract missing');

add('function p1aQueueWorkerState_(',`function p1aQueueWorkerState_(appId,worker){const q=SH_(JC.S.Q),m=hm_(q);if(!m['Application ID']||!m['Worker Type']||!m['Status']||q.getLastRow()<2)return{state:'NONE',jobId:''};let terminal='NONE',jobId='';for(let r=2;r<=q.getLastRow();r++){if(String(q.getRange(r,m['Application ID']).getDisplayValue())!==String(appId))continue;if(String(q.getRange(r,m['Worker Type']).getDisplayValue())!==String(worker))continue;const st=String(q.getRange(r,m['Status']).getDisplayValue()||'').toLowerCase(),jid=m['Queue Job ID']?String(q.getRange(r,m['Queue Job ID']).getDisplayValue()||''):'';if(['queued','running','retry_wait','retry','pending'].includes(st))return{state:'ACTIVE',jobId:jid,status:st};if(st==='succeeded'){terminal='SUCCEEDED';jobId=jid;}else if(st==='failed'&&terminal==='NONE'){terminal='FAILED';jobId=jid;}}return{state:terminal,jobId:jobId};}`);
add('function p1aTailoringNeedsResume_(',`function p1aTailoringNeedsResume_(a){a=a||{};return String(a['Decision']||'')==='Apply'&&String(a['Status']||'')==='Tailoring'&&!String(a['Resume Version ID']||'')&&String(a['Vacancy Status']||'').toUpperCase()!=='CLOSED'&&(Number(a['JD Completeness %']||0)>=70||String(a['JD Snapshot Status']||'').indexOf('Verified')>=0);}`);

replaceFn('p1aE2EContinuationTick_',`function p1aE2EContinuationTick_(){const aSheet=SH_(JC.S.A),last=Math.min(aSheet.getLastRow(),250);if(last<2)return{status:'NO_ROWS'};for(let r=2;r<=last;r++){const a=obj_(aSheet,r),id=String(a['Application ID']||'');if(!id||String(a['Decision']||'')!=='Apply')continue;if(String(a['Vacancy Status']||'').toUpperCase()==='CLOSED'){const closed=p1aPropagateClosedVacancy_(id,'E2E continuation v2 closed-vacancy guard');upsertWorkerState_('p1a_e2e_continuation_last_result','PASS',JSON.stringify({applicationId:id,result:closed}).slice(0,1500));return{status:'CLOSED_PROPAGATED',applicationId:id,result:closed};}if(!p1aTailoringNeedsResume_(a))continue;const qs=p1aQueueWorkerState_(id,'RESUME_GENERATE');if(qs.state==='ACTIVE'){const out={applicationId:id,status:'RESUME_ACTIVE',queue:qs};upsertWorkerState_('p1a_e2e_continuation_last_result','PASS',JSON.stringify(out).slice(0,1500));return out;}if(qs.state==='SUCCEEDED'){const out={applicationId:id,status:'STALL_SUCCEEDED_WITHOUT_RESUME_ARTIFACT',queue:qs};upsertWorkerState_('p1a_e2e_continuation_last_result','STALL',JSON.stringify(out).slice(0,1500));return out;}const input=hash_(id+'|'+String(a['JD Fit Score']||'')+'|'+String(a['JD Completeness %']||'')+'|p1a-e2e-continuation-v2'),job=enqueue_(id,'RESUME_GENERATE',{source:'p1a-e2e-continuation-v2'},input);if(!job)throw new Error('DETERMINISTIC:P1A_E2E_RESUME_ENQUEUE_FAILED');const after=p1aQueueWorkerState_(id,'RESUME_GENERATE'),out={applicationId:id,status:'RESUME_ENQUEUED',queueJobId:job,queue:after};upsertWorkerState_('p1a_e2e_continuation_last_result',after.state==='ACTIVE'?'PASS':'FAIL',JSON.stringify(out).slice(0,1500));if(after.state!=='ACTIVE')throw new Error('DETERMINISTIC:P1A_E2E_RESUME_QUEUE_READBACK_FAILED');return out;}upsertWorkerState_('p1a_e2e_continuation_last_result','NO_ELIGIBLE_STALL','bounded scan complete');return{status:'NO_ELIGIBLE_STALL'};}`);
replaceFn('p1aE2EContinuationSelfTest_',`function p1aE2EContinuationSelfTest_(){const c=[];c.push(p1aTailoringNeedsResume_({'Decision':'Apply','Status':'Tailoring','Resume Version ID':'','Vacancy Status':'OPEN','JD Completeness %':95})===true);c.push(p1aTailoringNeedsResume_({'Decision':'Apply','Status':'Tailoring','Resume Version ID':'V1','Vacancy Status':'OPEN','JD Completeness %':95})===false);c.push(p1aTailoringNeedsResume_({'Decision':'Apply','Status':'Tailoring','Resume Version ID':'','Vacancy Status':'CLOSED','JD Completeness %':95})===false);c.push(['queued','running','retry_wait','retry','pending'].length===5);if(c.some(x=>!x))throw new Error('P1-A E2E continuation v2 self-test failed '+JSON.stringify(c));return{pass:true,total:c.length,contract:'P1-A-E2E-CONTINUATION-2'};}`);
replaceFn('runP1AE2EContinuationSelfTest',`function runP1AE2EContinuationSelfTest(){const x=p1aE2EContinuationSelfTest_();upsertWorkerState_('p1a_e2e_continuation_self_test',x.pass?'PASS':'FAIL',JSON.stringify(x));upsertWorkerState_('p1a_e2e_continuation_contract_version','P1-A-E2E-CONTINUATION-2','Bounded queue-contract self-heal for stalled Apply/Tailoring journeys');return x;}`);
append('phase1HealthTick','runP1AE2EContinuationSelfTest();',`try{runP1AE2EContinuationSelfTest();}catch(e){upsertWorkerState_('p1a_e2e_continuation_self_test','FAIL',String((e&&e.stack)||e).slice(0,1500));}try{p1aE2EContinuationTick_();}catch(e){upsertWorkerState_('p1a_e2e_continuation_last_result','FAIL',String((e&&e.stack)||e).slice(0,1500));}`);

for(const token of ['function p1aQueueWorkerState_(','function p1aTailoringNeedsResume_(','function p1aE2EContinuationTick_(','function p1aE2EContinuationSelfTest_(','function runP1AE2EContinuationSelfTest()','p1a_e2e_continuation_self_test','p1a_e2e_continuation_last_result','P1-A-E2E-CONTINUATION-2'])if(!s.includes(token))throw new Error('E2E continuation v2 wiring missing '+token);
if(s.includes('const n=orchestrateOne_(id)'))throw new Error('Unsafe v1 orchestrator invocation still present');
if(s!==before)fs.writeFileSync(file,s);
console.log(JSON.stringify({status:'PASS',file:target,changed:s!==before,contract:'P1-A-E2E-CONTINUATION-2',boundedRows:250,maxAppsPerHealthTick:1,directQueueContract:true,orchestratorDependency:false},null,2));
