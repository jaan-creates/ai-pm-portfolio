import fs from 'node:fs';
import path from 'node:path';
import {spawnSync} from 'node:child_process';

const root=process.argv[2]||'.janu-live';
const files=fs.readdirSync(root).filter(f=>f.endsWith('.gs')||f.endsWith('.js'));
const target=files.find(f=>{const t=fs.readFileSync(path.join(root,f),'utf8');return t.includes('function sourceFreshnessHealth_(')&&t.includes('const P12');});
if(!target)throw new Error('Daily Sourcing health target not found');
const file=path.join(root,target);let s=fs.readFileSync(file,'utf8');
function rangeOf(name){const start=s.indexOf('function '+name+'(');if(start<0)return null;const open=s.indexOf('{',start);let d=0,q=null,e=false,line=false,block=false;for(let i=open;i<s.length;i++){const c=s[i],n=s[i+1]||'';if(line){if(c==='\n')line=false;continue;}if(block){if(c==='*'&&n==='/'){block=false;i++;}continue;}if(q){if(e){e=false;continue;}if(c==='\\'){e=true;continue;}if(c===q)q=null;continue;}if(c==='/'&&n==='/'){line=true;i++;continue;}if(c==='/'&&n==='*'){block=true;i++;continue;}if(c==='"'||c==="'"||c==='`'){q=c;continue;}if(c==='{')d++;else if(c==='}'&&--d===0)return{start,end:i+1};}throw new Error('Unterminated '+name);}
function replaceFn(name,code){const r=rangeOf(name);if(!r)throw new Error('Missing '+name);s=s.slice(0,r.start)+code+s.slice(r.end);}
const helpers=`function sourcingScheduledSuccess_(status,trigger){return String(status||'')==='SUCCEEDED'&&String(trigger||'').trim()==='Daily PM Sourcing Worker';/* SOURCING-HEALTH-BLOCKER-PRECEDENCE-001 / SOURCING-RECOVERY-NOT-LIVENESS-001 */}
function sourcingExpectedGapDates_(anchorYmd,scheduledYmds,lookbackWeekdays){const seen={};for(const x of scheduledYmds||[])seen[String(x||'')]=true;const d=new Date(String(anchorYmd||'')+'T12:00:00Z'),out=[];if(isNaN(d.getTime()))return out;let n=0,guard=0;while(n<Number(lookbackWeekdays||5)&&guard<20){const day=d.getUTCDay(),ymd=d.toISOString().slice(0,10);if(day>=1&&day<=5){n++;if(!seen[ymd])out.push(ymd);}d.setUTCDate(d.getUTCDate()-1);guard++;}return out;/* SOURCING-DATE-GAP-001 */}`;
if(!s.includes('function sourcingScheduledSuccess_(')){const i=s.indexOf('function sourceFreshnessHealth_(');s=s.slice(0,i)+helpers+'\n'+s.slice(i);}else if(!s.includes('function sourcingExpectedGapDates_(')){const i=s.indexOf('function sourceFreshnessHealth_(');s=s.slice(0,i)+helpers.split('\n').slice(1).join('\n')+'\n'+s.slice(i);}
replaceFn('sourceFreshnessHealth_',`function sourceFreshnessHealth_(){
  ensureP12Sheets_();const s=SH_(P12.SHEETS.SOURCING);if(s.getLastRow()<2){healthSet_('Daily Sourcing','DEGRADED','OPEN','NO_SOURCING_RUN','No sourcing run records',0,'');return false;}
  const m=hm_(s),v=s.getDataRange().getValues();let lastScheduled=null,lastAny=null;const scheduledDays=[];
  for(let i=1;i<v.length;i++){const status=String(v[i][m['Status']-1]||''),trigger=String(v[i][m['Trigger']-1]||''),d=status==='SUCCEEDED'?parseDateish_(v[i][m['Finished At']-1]):null;if(d&&(!lastAny||d>lastAny))lastAny=d;if(d&&sourcingScheduledSuccess_(status,trigger)){if(!lastScheduled||d>lastScheduled)lastScheduled=d;scheduledDays.push(Utilities.formatDate(d,'Asia/Kolkata','yyyy-MM-dd'));}}
  const now=now_(),day=Number(Utilities.formatDate(now,'Asia/Kolkata','u')),hour=Number(Utilities.formatDate(now,'Asia/Kolkata','H')),weekday=day<=5,age=lastScheduled?(now-lastScheduled)/3600000:999,ok=!weekday||age<=24;
  const anchor=new Date(now.getTime());if(hour<10)anchor.setDate(anchor.getDate()-1);const anchorYmd=Utilities.formatDate(anchor,'Asia/Kolkata','yyyy-MM-dd'),recentGaps=sourcingExpectedGapDates_(anchorYmd,scheduledDays,5);
  if(ok)closeCircuit_('Daily Sourcing','Latest scheduled sourcing age '+age.toFixed(1)+'h');else openCircuit_('Daily Sourcing','SOURCING_SCHEDULER_STALE','No successful scheduled weekday sourcing cycle within 24h; recovery/manual success does not prove scheduler liveness');
  upsertWorkerState_('sourcing_scheduler_recent_gaps',recentGaps.length?'GAPS':'CLEAN',JSON.stringify({contract:'SOURCING-DATE-GAP-001',anchor:anchorYmd,lookbackWeekdays:5,missingExpectedDates:recentGaps}).slice(0,1500));
  upsertWorkerState_('sourcing_scheduler_health',ok?'PASS':'STALE',JSON.stringify({contract:'SOURCING-HEALTH-BLOCKER-PRECEDENCE-001',latestScheduled:lastScheduled?lastScheduled.toISOString():'',latestAnySuccess:lastAny?lastAny.toISOString():'',weekday:weekday,ageHours:age,recentGapCount:recentGaps.length}).slice(0,1500));return ok;
}`);
for(const token of ['SOURCING-HEALTH-BLOCKER-PRECEDENCE-001','SOURCING-RECOVERY-NOT-LIVENESS-001','SOURCING-DATE-GAP-001','SOURCING_SCHEDULER_STALE','function sourcingScheduledSuccess_(','function sourcingExpectedGapDates_(','sourcing_scheduler_recent_gaps'])if(!s.includes(token))throw new Error('Sourcing health contract missing '+token);
fs.writeFileSync(file,s);const ck=spawnSync(process.execPath,['--check',file],{encoding:'utf8'});if(ck.status!==0)throw new Error(ck.stderr);console.log(JSON.stringify({status:'PASS',contract:'SOURCING-HEALTH-BLOCKER-PRECEDENCE-001',dateGapContract:'SOURCING-DATE-GAP-001',file:target},null,2));