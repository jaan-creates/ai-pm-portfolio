# EDGE_CASES — mined from career-ops (8c941ee) + hiring-agent (4db8655)

Paths are relative to `_mined/<repo>/`. Tags: `TR` = technical_robustness · `UX` = ux_failure · `PC` = product_completeness_gap.
Verdicts are for the Job-Copilot product (hosted app, Lead/Senior PM job seekers).

## Domain: JD parsing & fit assessment

| case_id | repo | tag | description | their_handling | evidence | verdict | pm_native_reframe |
|---|---|---|---|---|---|---|---|
| JD-01 | career-ops | UX | Dead/expired posting reaches the evaluator and wastes a full evaluation + report + PDF on phantom content | Liveness gate before Block A: URL inputs must be verified live first; closed → stop, mark pipeline entry, generate nothing | modes/oferta.md:5-16 | adopt | — |
| JD-02 | career-ops | TR | Anti-bot interstitial (Cloudflare "Just a moment", hCaptcha) has a short body and no apply button, so naive length heuristics classify a LIVE job as expired and permanently filter it out of future scans | Dedicated bot-challenge pattern list checked before all content heuristics; result is `uncertain`, never `expired`; 403/503 likewise `uncertain` ("access blocked"), only 404/410 are `expired` | liveness-core.mjs:24-40, 83-98 | adopt | — |
| JD-03 | career-ops | TR | Dead permalink 301s to a generic search/listing page that shows Apply buttons on OTHER jobs' cards → false "active" | Job-ID token (req id/UUID) extracted from requested URL; if final URL lost the token, classify `uncertain` (portal migrations 301 live jobs too — a false "expired" permanently drops a real job) | liveness-core.mjs:64-73, 110-124 | adopt | — |
| JD-04 | career-ops | TR | Liveness is genuinely three-valued; forcing binary live/dead poisons downstream state | `active` / `expired` / `uncertain` with machine-readable reason codes (`http_gone`, `bot_challenge`, `redirected_off_posting`, `insufficient_content`, `no_apply_control`) | liveness-core.mjs:83-140 | adopt | — |
| JD-05 | career-ops | TR | Expired-notice pages still contain the word "Apply" in nav/related-jobs, so apply-text detection alone false-positives | Hard-expired body patterns are checked before apply-control detection — expired signals win over generic Apply text | liveness-core.mjs:105-108, 126-128; AGENTS.md ("expired signals win over generic Apply text", Main Files table) | adopt | — |
| JD-06 | career-ops | TR | Non-English postings (Polish, German, French job boards) have no recognized apply/expired phrases → misclassified | Multilingual pattern lists with per-language scar comments (pracuj.pl "Aplikuj", German "Stelle besetzt", French "offre expirée") | liveness-core.mjs:13-15, 46-61 | adapt | PM product is English-first at launch; keep the pattern-list architecture (data-driven, extensible per market) but ship EN-only and add languages on demand |
| JD-07 | career-ops | UX | Page with only nav/footer (JD body failed to render) parsed as if it were the JD | Minimum content threshold (300 chars) → `expired: insufficient_content`; bodies below it never treated as evaluable JD text | liveness-core.mjs:63, 135-137 | adopt | — |
| JD-08 | career-ops | TR | WebSearch/WebFetch report stale cached pages as live | "NEVER trust WebSearch/WebFetch to verify if an offer is still active — ALWAYS use Playwright"; headless fallback must stamp the report `**Verification:** unconfirmed (batch mode)` | AGENTS.md:362-367 | adapt | Hosted app: server-side headless browser check with the same three-state classifier; surface "verified live at {timestamp}" vs "unverified" as a first-class badge on every job card |
| JD-09 | career-ops | UX | Pasted JD text (no URL) cannot be liveness-checked; silently pretending it was verified misleads the user | Explicitly note "liveness cannot be verified" and proceed — absence of check is stated, not hidden | modes/oferta.md:14 | adopt | — |
| JD-10 | career-ops | PC | User keeps re-evaluating companies they already decided never to apply to | Blacklist gate before evaluation: case/punctuation-insensitive company match against user-curated do-not-apply list; on hit, stop and ask (explicit override allowed, noted in report); never auto-populated, never affects scores | modes/oferta.md:18-25 | adopt | — |
| JD-11 | career-ops | TR | LLM company research fans out into open-ended browsing and burns unbounded tokens per evaluation | Bounded research budget: hard cap 5 WebSearch queries for comp+legitimacy combined; stop early; missing data explicitly marked unavailable, never guessed; no research subagents ("can burn tens of millions of tokens on one run") | modes/oferta.md:27-38; modes/_shared.md:205-211 | adopt | — |
| JD-12 | career-ops | UX | Posting's structured location says "remote" but JD body demands office attendance — candidate finds out at interview | Geo-mismatch check: contradiction flagged only for binding attendance language; negations/optional events/benefits boilerplate excluded; silence ≠ contradiction; evidence quoted verbatim, never paraphrased | modes/oferta.md:59-72 | adopt | — |
| JD-13 | career-ops | UX | Advertised salary blended with researched estimates until user can't tell what the JD actually said | Advertised figure is always row 1 of the comp table, verbatim, source "JD"; market rows follow; `advertised_comp` in machine summary is verbatim-or-null, never estimated | modes/oferta.md:169-175, 489 | adopt | — |
| JD-14 | career-ops | UX | Advertised comp treated as take-home: "up to", OTE, bonus-inclusive, 13th-salary figures inflate expectations | Company-type taxonomy (10 categories) → comp-reliability tier; low-reliability trigger phrases listed; unknown company type defaults to conservative `Low`; brand vs legal hiring entity classified separately | modes/_shared.md:115-145; modes/oferta.md:105-154 | adopt | — |
| JD-15 | career-ops | UX | JD written for the org the company *wants to become* — "AI transformation" language on top of infrastructure that doesn't exist; candidate discovers digitization-backlog reality after burning interviews | Buzzword/infrastructure mismatch: 3 signal classes (buzzword density vs role scope, tiny team owning org-wide transformation, legacy-industry base rate); flag only at 2+ of 3; note is descriptive, never prescriptive; does not change legitimacy tier | modes/oferta.md:264-278 | adopt | Directly PM-relevant: "Head of AI Product" postings at legacy firms are this exact trap for senior PMs |
| JD-16 | career-ops | UX | Contractor-classified role masquerading as employment (1099/T4A/IR35/"invoice for services"); candidate learns at tax time | Employment-classification signal: jurisdiction-specific term table (extensible data, not logic) + structural check requiring explicit contractor wording AND a corroborating omission ("contract position" alone never triggers); orthogonal to ghost-job tier | modes/oferta.md:245-262 | adopt | — |
| JD-17 | career-ops | TR | JD copy-pasted from another country's template (Canada posting listing 401(k)/W-2) → wrong benefits assumptions | Country-mismatch detector with strong vs corroborating-only markers ("PTO" or bare "ESA" can never trigger alone — documented ambiguity guards); framed as template-error, not fraud | modes/oferta.md:280-295 | adapt | Keep the two-tier marker design; for PM users the analogous check is comp-band/level-terminology mismatches (e.g. US level names on a UK posting) |
| JD-18 | career-ops | TR | Job board's displayed location differs from employer's own page (mis-scrape or wrong region tag on cross-post) | Flag only when BOTH sources available AND confirmed same requisition via matching req/job ID (same title+company is not enough), AND countries differ (city-level differences too ambiguous) | modes/oferta.md:297-311 | skip | Requires fetching two sources per job; for v1 the single-source location + JD-body geo-mismatch check (JD-12) covers the dominant failure; revisit if users report cross-posting errors |
| JD-19 | career-ops | UX | Ghost-job scoring smears into fit scoring, or missing data reads as "suspicious" | Legitimacy (Block G) is a separate qualitative 3-tier assessment that never affects the 1-5 fit score; explicit edge-case table: government/evergreen/executive roles get adjusted age thresholds; **no posting date + no other signals → default "Proceed with Caution", NEVER "Suspicious" without evidence** | modes/_shared.md:87-113; modes/oferta.md:324-330 | adopt | — |
| JD-20 | career-ops | UX | Every legitimacy signal has innocent explanations; presenting findings as accusations poisons trust | Mandatory ethical framing: present signals + legitimate explanations, user decides; reposting/salary-omission signals explicitly weighted low/medium with caveats | modes/_shared.md:96-113; modes/oferta.md:213 | adopt | — |
| JD-21 | career-ops | UX | A 4.5+ overall score can bury a failed culture screen | Contradicted culture requirement caps that dimension at 2/5 and forces an explicit warning line: "High technical fit, unconfirmed/poor culture fit — verify before applying"; no evidence ≠ contradiction (defaults to neutral 3) | modes/_shared.md:79-85 | adopt | — |
| JD-22 | career-ops | TR | Invalid config value for model-spend tier silently degrades behavior | Unknown `spend_tier` → fall back to `standard` and tell the user once; model tier never changes output format ("output parity") | modes/_shared.md:39, 57 | adopt | — |
| JD-23 | career-ops | TR | Cheap-model evaluator returns a malformed report; saving it corrupts tracker + reports | `validateEvaluationShape`: regex-verify all 7 blocks + score-summary sentinel block + required fields + score numeric in [0,5]; on failure NOTHING is saved, user told to retry/lower temperature/use stronger model | gemini-eval.mjs:160-199, 320-326 | adapt | Adopt validate-before-persist; replace "user retries manually" with one automatic retry at lower temperature, then a visible "evaluation failed" state in the UI |
| JD-24 | career-ops | TR | API error messages can leak the API key into logs | Error message sanitized: `err.message.split(apiKey).join('[REDACTED]')` before printing; quota/rate errors get actionable hints | gemini-eval.mjs:309-317 | adopt | — |
| JD-25 | career-ops | TR | Scanned postings link to URL shorteners or domains unrelated to the claimed company (scam/ghost signals) | Trust validator: 0-100 penalty score (invalid URL −50, missing apply URL −40, shortener domain −25, company↔domain mismatch −15); ATS-hosted domains allowlisted from mismatch check; **"Never drops jobs — flag only"**; can't-evaluate → no flag | providers/_trust-validator.mjs:3-16, 50-56, 117, 146-217 | adopt | — |
| JD-26 | career-ops | TR | Slow/hung job-board endpoints stall the whole scan | Shared HTTP helper: AbortController timeout (10s default); error object carries status, body, and `retry-after` header for callers | providers/_http.mjs:4, 17-45 | adopt | — |
| JD-27 | career-ops | TR | WAF/CDN bot management blocks the tool's default User-Agent outright (seen live: Glints firewall, Cloudflare-gated Workday tenant) | Single shared browser-like UA constant so every provider working around a block bumps one string ("instead of drifting Chrome versions independently per file") | providers/_http.mjs:7-15 | adopt | — |
| JD-28 | hiring-agent | TR | Reasoning models leak `<think>…</think>` blocks into JSON output | Strip think-block span before any JSON parsing | llm_utils.py:24-29 | adopt | — |
| JD-29 | hiring-agent | TR | LLM wraps JSON in ```json fences and/or surrounds it with prose | Strip fences, then slice from first `{` to last `}` before `json.loads` | llm_utils.py:31-37; pdf.py:111-116 | adopt | — |
| JD-30 | hiring-agent | TR | JSON still fails to parse after repair | Log raw response + return None — **no retry, no repair reprompt**; whole document extraction then aborts | pdf.py:127-130 | adapt | Gap worth fixing, not copying: add bounded retry (1-2 attempts, reprompt with the parse error), then per-section failure status in UI |
| JD-31 | hiring-agent | TR | One section failing silently yields a partial/invalid parsed document presented as complete | Abort-all with explanatory comment: "Failed to extract {section}. Aborting extraction to prevent partial/invalid resume data" | pdf.py:295-299 | adapt | For a hosted PM app invert it: keep good sections, mark failed ones "couldn't read this section — edit manually", never silently show a partial parse as complete |
| JD-32 | hiring-agent | TR | Rate-limited API (free-tier quota) kills long runs | 5 retries, exponential backoff (base 10s, cap 120s), parses API's own `retry_in Xs` hint and prefers it when shorter, ±20% jitter; after exhaustion re-raises — comment: "surfaces unrecoverable quota errors… instead of silently failing or returning bad data" | models.py:335-391 | adopt | — |
| JD-33 | hiring-agent | UX | Missing API key silently falls back to a weaker local model — user gets degraded quality without knowing | `logger.warning` then fallback to Ollama | llm_utils.py:54-59 | adapt | Hosted product must never silently swap models: fail the run visibly or show "evaluated with fallback model" on the result |
| JD-34 | hiring-agent | TR | Local-model default context window truncates long resumes silently | `num_ctx` forced to 32768 on every Ollama call | models.py:293-294 | adapt | PM-native: JD+resume+rubric prompts must be token-budgeted; measure and truncate the *JD boilerplate*, never the user's resume content, and surface "JD truncated" when it happens |
| JD-35 | hiring-agent | TR | Structured-output schema not enforced → free-form answers | `format = Model.model_json_schema()` passed on every extraction and evaluation call | pdf.py:101-103; evaluator.py:76 | adopt | — |

## Domain: LLM-output normalization (feeds parsing + scoring)

| case_id | repo | tag | description | their_handling | evidence | verdict | pm_native_reframe |
|---|---|---|---|---|---|---|---|
| NM-01 | hiring-agent | TR | LLM names the same section differently across runs (`work` / `work_experience` / `experience`; `awards` / `achievements` / `honors_and_awards`) | Alias fallback chains on every section lookup | transform.py:12-16, 22-28, 74-85 | adopt | Same aliasing needed for JD sections: "Requirements"/"What you'll need"/"About you" |
| NM-02 | hiring-agent | TR | Field types drift: description arrives as list or string; technologies as comma-string or array; skills as flat string-list or category objects | Type-sniff then coerce (join list, split string, wrap flat list into a category) | transform.py:179-181, 326-331, 351-361 | adopt | — |
| NM-03 | hiring-agent | TR | LLM stuffs a date RANGE into `startDate` ("Jan-Mar 2021"); year ranges, "onwards", single months all appear | Detect month tokens in startDate → route through `parse_date_range` handling "onwards"→Present, month-ranges, year-ranges; unparseable → (None, None), never a crash | transform.py:183-206, 412-483 | adopt | — |
| NM-04 | hiring-agent | TR | Resume idiom "Project Name \| React, Node" packs skills into the name field | Split on pipe, strip, promote right side to skills list | transform.py:317-324 | adopt | PM resumes do the same with "Product — B2B SaaS, 0→1"; same split-and-promote pattern |
| NM-05 | hiring-agent | TR | LLM omits `network` on profile links | Infer network + username from URL domain (mapping table incl. x.com→X); LinkedIn/StackOverflow username position differs per site | transform.py:98-172 | adopt | — |
| NM-06 | hiring-agent | UX | Normalization itself throws → whole parse dies | Catch-all returns the raw data unchanged (fail-open) | transform.py:93-95 | adapt | Fail-open is right, but tag the record "unnormalized" so downstream scoring can lower confidence instead of treating it as clean |
| NM-07 | hiring-agent | TR | "Latest job" derived by `work[0]` with comment "Assuming sorted by date" — false for functionally-ordered resumes | Unguarded assumption, no sort | transform.py:596-607 | adapt | Sort by parsed endDate (Present first) before deriving "current role" — critical for senior PMs whose resumes often lead with a flagship role, not the latest |
| NM-08 | hiring-agent | TR | Corrupt or empty dev cache poisons subsequent runs | Cache validated on load (must parse + contain core content); invalid → warn, delete file, reprocess from source | score.py:227-244, 272-294 | adopt | Same self-healing pattern for cached JD parses |
| NM-09 | hiring-agent | TR | "Parsed successfully" but every section is null | `is_valid_resume_data`: at least one core section must be non-None before the result is used or cached | score.py:191-202, 258-267 | adopt | — |

## Domain: scoring guards (fit assessment)

| case_id | repo | tag | description | their_handling | evidence | verdict | pm_native_reframe |
|---|---|---|---|---|---|---|---|
| SC-01 | hiring-agent | TR | LLM awards more points than a category's max | `min(score, max)` cap at render time + printed warning naming the capped category | score.py:42-55 | adopt | — |
| SC-02 | hiring-agent | TR | Bonus/deduction arithmetic pushes total past the possible maximum | Total clamped to max+bonus (120) with warning; named constants bound the range (−20..120) | score.py:57-69; evaluator.py:9-11 | adopt | — |
| SC-03 | hiring-agent | TR | Schema-level bounds beat prompt-level pleading | Pydantic `Field(ge=0)`, bonus `le=20`, evidence `min_length=1`, strengths/improvements `min_items=1 max_items=5` — invalid LLM output fails validation instead of flowing downstream | models.py:218-249 | adopt | — |
| SC-04 | career-ops | UX | Score bands without actions leave the user staring at a number | Interpretation bands tied to recommendations (4.5+ apply immediately … <3.5 recommend against); below 4.0 the assistant explicitly discourages applying (ethical-use rule) | modes/_shared.md:72-76; AGENTS.md:354 | adopt | — |

## Domain: application tracking & state machine

| case_id | repo | tag | description | their_handling | evidence | verdict | pm_native_reframe |
|---|---|---|---|---|---|---|---|
| ST-01 | career-ops | TR | Freeform status strings ("**Applied** 2026-01-03", localized variants) make the tracker unqueryable | Canonical state registry in one YAML file (id + label + aliases + description), shared by writer and dashboard reader; status cell must be exactly one canonical value — no bold, no dates, no extra text (those have their own columns) | templates/states.yml:1-62 | adopt | In a hosted app this is an enum column — but keep the alias map for importing users' existing spreadsheets |
| ST-02 | career-ops | PC | Prose docs duplicate the state list and drift from the machine source (`Hired` exists in states.yml but is missing from the AGENTS.md states table) | states.yml declared "source of truth"; drift still happened in the doc copy | templates/states.yml:58-62 vs AGENTS.md (Canonical States table, 8 rows) | adopt | Single machine-readable state definition; anything user-facing renders from it |
| ST-03 | career-ops | TR | Multiple agents/processes hand-editing one shared tracker file corrupt it | One canonical write path (#1428): all modes call `set-status.mjs` instead of editing markdown; strict state validation happens before the file is touched | set-status.mjs:3-8, 146-158 | adopt | Same principle as "all writes go through the API layer" — worth stating explicitly because LLM agents will otherwise edit stored data directly |
| ST-04 | career-ops | TR | Duplicate row IDs (from a since-fixed merge bug, #1704) make "update row 42" a silent coin flip over which company gets edited | Refuse to guess: ambiguous numeric selector fails with a numbered candidate list and exit code 3; `--role` fuzzy-narrows; company selector as alternative | set-status.mjs:13-21, 185-196 | adopt | — |
| ST-05 | career-ops | TR | Row ID copied from a report filename while tracker drifted → status lands on the wrong application | Report-link mismatch guard: numeric selector cross-checked against the row's report link IDs; mismatch → refuse with repair options; `--force` records an explicit override | set-status.mjs:272-288 | adopt | — |
| ST-06 | career-ops | TR | CLI flag parsing: `--note --dry-run` consumes the next flag as the note value — silently disabling dry-run and turning a preview into a real write | Flag values that start with `--` are rejected as missing values | set-status.mjs:80-88 | adopt | — |
| ST-07 | career-ops | TR | Re-running the same status command appends the note twice; naive substring dedupe suppresses genuinely new notes ("sent" inside "sent CV") | Delimiter-aware idempotency: note counts as present only as a whole "; "-delimited entry; re-runs always safe, exit 0 on no-op | set-status.mjs:300-320 | adopt | — |
| ST-08 | career-ops | TR | Two writers race on the tracker file | Shared advisory lock keyed by hashed tracker path; owner.json records PID; stale-lock recovery checks process liveness (EPERM counts as alive) with mtime fallback (10 min); lock-timeout gets its own exit code (4 = retry later) distinct from non-retryable config errors; release is idempotent and hooked on process exit | set-status.mjs:222-250; tracker-utils.mjs:139-233 | adapt | Hosted app: this is a DB transaction — but the *distinction between retryable-busy and non-retryable failures* in the API contract carries over |
| ST-09 | career-ops | UX | A read-only preview blocking (or being blocked by) real writers | `--dry-run` never acquires the exclusive lock — "a stale read is acceptable for a preview" | set-status.mjs:224-228 | adopt | — |
| ST-10 | career-ops | TR | Status update rewrites the whole file and mangles unrelated rows | Only the matched row's Status/Notes cells change; "every other byte of the tracker round-trips untouched"; writes are atomic (temp + rename); `rebuildRow` handles the trailing-`\|` split pitfall | set-status.mjs:29-31, 292-333; tracker-utils.mjs:17-22 | adopt | — |
| ST-11 | career-ops | TR | Side-effect hooks fire on idempotent re-runs (re-setting Applied seeds a duplicate follow-up) | `followupSeedCandidate` emitted only on an actual *transition into* Applied, never on a no-op re-run | set-status.mjs:347-350 | adopt | Classic state-machine rule: effects on transitions, not on states |
| ST-12 | career-ops | TR | TSV row with score/status columns swapped merges silently and corrupts the tracker | Identify score vs status *by content, not position* (`looksLikeScoreCell`: `X.X/5` or sentinel `N/A`/`—`/`-`); undecidable rows skipped loudly (#1427); backfilled no-evaluation rows must use a recognized sentinel, never blank (#1799) | merge-tracker.mjs:324-361; tracker-parse.mjs:56-82; AGENTS.md:438-442 | adopt | — |
| ST-13 | career-ops | TR | Same application enters tracker twice (batch + manual, rescan) | Dedup key = normalized company + fuzzy role match + report number; duplicate with higher score updates in place; hard rule: never create a new row if company+role exists | merge-tracker.mjs:2-15; AGENTS.md:415 | adopt | — |
| ST-14 | career-ops | TR | Merging duplicate rows loses progress: the discarded row held the more advanced status | Status advancement ranking; keep highest score but preserve the most advanced status; notes merged; explicit ordering decision documented: Applied(3) > Rejected(1) because "active application > terminal state" | dedup-tracker.mjs:1-50 | adopt | Ranking states by pipeline progress is the PM-native analytics backbone too (funnel position) |
| ST-15 | career-ops | TR | Parallel evaluation workers each compute next report number = max+1 → collision (the "#749 race") | Atomic allocator: reservation sentinels created O_CREAT\|O_EXCL under the tracker lock; occupancy = report files + sentinels + tracker row IDs + tracker report links; stale sentinels GC'd after 4h; collisions restart past the collision leaving harmless permanent gaps | reserve-report-num.mjs:1-16, 34-36; AGENTS.md:404 | adapt | Hosted app: DB sequence/UUID solves it — but the *lesson* (LLM agents must never self-assign IDs; IDs come from one allocator) transfers directly |
| ST-16 | career-ops | TR | Batch runner records results in its own state file but never writes back to the inbox → processed items re-surface forever and get re-evaluated (duplicate reports, rows, spend) | `reconcile-pipeline.mjs`: moves completed/skipped entries from "pending" to "processed" with report link; idempotent — already-moved entries are no-ops, entries present in both sections are deduped | reconcile-pipeline.mjs:2-21 | adopt | The generic form: every async pipeline needs a reconciler that closes the loop between work-done log and work-queue |
| ST-17 | career-ops | UX | Same role re-listed with a new URL defeats URL dedup; user re-evaluates a ghost | Repost detector (#1205): group scan history by company, fuzzy-match titles, flag 2+ appearances with different URLs in 90 days; only `added` rows count — `skipped_expired`/`skipped_invalid_url` rows are dead postings, not reposts; strict ISO date validation with round-trip check | detect-reposts.mjs:2-22, 46-52 | adopt | Repost count is also a *legitimacy* input (feeds Block G) — wire the two together as career-ops does |
| ST-18 | career-ops | TR | Agency re-posts the same JD with the employer name stripped — URL dedup AND company+role dedup both miss it | Content fingerprint (#1597): 64-bit SimHash over 3-token shingles of normalized JD text; ≥0.92 similarity (≤5/64 bits differ) within 90 days from a *different* company = cross-listing; same-company matches excluded (that's repost territory); no usable body → empty fingerprint, never matched ("no body, no signal, no false positives") | fingerprint-core.mjs:1-32, 122-146 | adopt | Highest-value dedup idea in either repo for a PM product: agencies blanket senior-PM roles; also dirt-cheap (zero LLM calls) |
| ST-19 | career-ops | TR | Degenerate text (unspaced CJK normalizes to one giant token) yields an all-zero SimHash that scores 1.0 against every other degenerate body | Explicit guard: <3 tokens → unfingerprintable, even when length check passes | fingerprint-core.mjs:63-67 | adopt | — |
| ST-20 | career-ops | TR | Agency-mediated posting where end employer is unknown: writing "Confidential" collides with real firms and breaks dedup | `?` as company (locale-invariant structural marker) + descriptor in notes; `via={Agency}` as a *tagged* extra TSV field (never positional — a single untagged extra keeps its legacy meaning); ambiguous extras rejected loudly; report slug `confidential-{agency}` is never renamed after the employer is revealed — header/YAML updated instead | modes/oferta.md:436, 496-497; AGENTS.md:440 | adopt | — |
| ST-21 | career-ops | TR | Report links break depending on which directory the tracker lives in | Additions always carry root-relative links; merge rewrites them relative to the tracker's own directory; normalization is idempotent; `--migrate` fixes legacy trackers (#760) | merge-tracker.mjs:52-64; AGENTS.md (Report link normalization) | skip | Hosted app stores entity references, not markdown paths; no equivalent failure exists |
| ST-22 | career-ops | TR | Tests/fixtures accidentally mutate the real user tracker | Every writer resolves the tracker path through one shared function honoring `CAREER_OPS_TRACKER` env override "so the real user tracker is never touched"; lock dir derived from hashed tracker path; env-supplied lock paths validated (inside tmpdir, mandatory name prefix) else fallback | dedup-tracker.mjs:23-26; tracker-utils.mjs:139-152 | adopt | — |
| ST-23 | career-ops | UX | Status cells polluted with markdown bold, dates, and dedupe markers from hand edits | `normalize-statuses.mjs` cleanup pass: maps aliases to canonical, strips bold/dates, relocates `DUPLICADO` info to notes; `--dry-run` supported; backup before write | normalize-statuses.mjs:1-30 | adapt | Hosted app prevents this at write time (enum), but an *import sanitizer* with exactly these rules is needed for spreadsheet migration |

## Domain: prompt & rubric edge cases

| case_id | repo | tag | description | their_handling | evidence | verdict | pm_native_reframe |
|---|---|---|---|---|---|---|---|
| PR-01 | hiring-agent | UX | LLM scores drift toward prestige/demographic signals | Explicit blindlist in BOTH system message and user prompt: scores must never depend on name, gender, school, GPA, location | prompts/templates/resume_evaluation_system_message.jinja:5-11; resume_evaluation_criteria.jinja:5-19 | adapt | Candidate-side equivalent: fit score must not depend on employer brand prestige or school unless the JD explicitly requires them — keeps gap analysis honest for PMs from non-FAANG backgrounds |
| PR-02 | hiring-agent | UX | Un-anchored rubrics let the LLM grade generously and inconsistently | Every category has banded anchors with *named concrete examples* at each band, including negative anchors ("Hacktoberfest alone = 3-5 points max", enumerated tutorial-project list: todo/calculator/weather apps) | resume_evaluation_criteria.jinja:34-114 | adopt | PM anchors: "ran agile ceremonies" ≠ leadership artifact; "launched feature" vs "owned 0→1 with P&L"; enumerate the PM-resume equivalents of todo-apps ("stakeholder management", "cross-functional collaboration" without outcomes) |
| PR-03 | hiring-agent | TR | LLM conflates similarly-named entities (GSoC vs Girl Script Summer of Code) and awards the wrong credit | Dedicated "PROGRAM DISTINCTIONS" prompt section forbidding the shorthand conflation | resume_evaluation_criteria.jinja:21-25 | adopt | PM confusion pairs need the same treatment: Product vs Project vs Program Manager; "led team of 12" (matrix) vs direct reports; ARR influenced vs owned |
| PR-04 | hiring-agent | TR | Evidence and conclusions contradict each other (evidence says "no open-source activity", key_strengths lists "open source contributions") | Cross-field consistency rules: if evidence states absence, the strength MUST NOT be listed | resume_evaluation_system_message.jinja:23 (final MANDATORY clauses) | adopt | Same rule wired between "gaps" and "strengths" sections of a fit report |
| PR-05 | hiring-agent | TR | Numeric constraints stated only in prose get violated | Defense in depth: caps stated in rubric prompt, restated as score-constraint block, enforced in pydantic schema (`le=20`, `ge=0`), clamped again in display code | resume_evaluation_criteria.jinja:155-164; models.py:218-249; score.py:42-69 | adopt | — |
| PR-06 | hiring-agent | TR | Prompt and schema drift apart: system message demands a `candidate_name` field that the pydantic schema doesn't define — model is told to violate its own output contract | Unhandled (live bug/latent conflict in repo) | resume_evaluation_system_message.jinja:31 vs models.py:244-249 | adapt | Don't hand-write output-shape instructions: generate the prompt's schema description from the same pydantic/zod model that validates the response |
| PR-07 | hiring-agent | TR | Model performs the wrong *task* (writes a resume summary instead of scoring; emits `<think>` blocks) | Observed-failure guardrails: "You are NOT writing a resume summary", "DO NOT RETURN A RESUME SUMMARY", "no <think> tags" — paired with code-side think-stripper as backstop | resume_evaluation_system_message.jinja:3; resume_evaluation_criteria.jinja:166; system_message.jinja:3; llm_utils.py:25-29 | adopt | Prompt guard + code backstop for the same failure is the pattern, not either alone |
| PR-08 | hiring-agent | TR | Resume date formats defeat extraction (en/em dashes, "to", "Present"/"Current"/"Ongoing", MM/YYYY vs Month Year) | Extraction prompt enumerates 8 explicit date-handling rules; code-side `parse_date_range` mirrors them as a backstop | prompts/templates/work.jinja:22-33; transform.py:412-483 | adopt | — |
| PR-09 | hiring-agent | TR | Big single-shot resume parse yields more invalid JSON than small focused ones | One prompt per section, each with an inline example JSON shape and a section-scoped system message; small schemas per call | prompts/templates/work.jinja:9-20; pdf.py:266-299 | adopt | Same decomposition for JD parsing: requirements / responsibilities / comp / logistics as separate extractions |
| PR-10 | career-ops | PC | One-size rubric ignores individual deal-breakers of senior candidates | User-profile scoring overrides loaded before scoring and winning over system defaults: block caps ("cap Block A at 3.0/5 if title contains Lead/Head/Principal"), forced decisions ("force SKIP if comp ceiling below $120K"), dimension rules, archetype→proof-point maps | batch/batch-prompt.md:52-62 | adopt | This is the core personalization surface for Lead/Senior PMs: user-defined hard filters (min scope, min comp, no-IC-roles) applied as rubric constraints, not post-filters |
| PR-11 | career-ops | TR | Prose reports can't be consumed by downstream scripts; re-parsing prose is fragile | Dual-output contract: human report + `Machine Summary` YAML fence with exact field names, strict enums, `[]` for empty lists, score numeric only, verbatim-or-null advertised comp, "do not invent missing data — set confidence Low" | batch/batch-prompt.md:299-345 | adopt | — |
| PR-12 | career-ops | UX | Risk signals scattered across report blocks; silence is ambiguous (not-checked vs checked-and-clear) | Risk Summary block: aggregation-only ("zero new judgment"), fixed row order, `— not evaluated` as first-class state so an all-✅ summary can be trusted; batch variant explicitly declares which signals that mode *cannot* evaluate | batch/batch-prompt.md:245-273; modes/oferta.md:334-362 | adopt | — |
| PR-13 | career-ops | TR | "Is cheap model X good enough for task Y?" decided by vibes; silent quality drift when routing changes | Golden-set eval (#1354): 10 synthetic labeled JDs spanning all 6 archetypes, deliberately favoring hybrid/ambiguous cases; metric = agreement-with-reference (not absolute correctness); archetype agreement gates (0.8), score agreement is tolerance-banded (±0.5) secondary; recorded fixtures give $0 deterministic CI replay; labels carry `provenance` | evals/README.md:1-112 | adopt | Build this before shipping any model-routing decision; PM golden set = 10 synthetic PM JDs (platform/growth/0→1/plg/enterprise hybrids) |
| PR-14 | career-ops | TR | Batch worker prompt breaking when shared mode files change | "This prompt is self-contained. Do not depend on any slash command, skill, or external mode file at runtime" | batch/batch-prompt.md:14 | adopt | Version-pin the full prompt per pipeline run; don't compose from live-mutable parts |
| PR-15 | career-ops | TR | JD file empty/missing at worker start | Fallback chain: local file → WebFetch → structured failure JSON and stop (orchestrator sees a parseable failure, not a crash) | batch/batch-prompt.md (Step 1 — Load the JD) | adopt | — |
| PR-16 | career-ops | PC | Report language, market vocabulary, and prompt language conflated | Two explicit axes: `language.output` (all human-facing prose) vs `language.modes_dir` (market vocabulary only), with a composition rule injected into every subagent prompt | batch/batch-prompt.md:19-31 | skip | English-first v1; the *separation of concerns* is worth remembering if market packs (DACH/India PM markets) ship later |

## Domain: resume scoring, tailoring & similarity

| case_id | repo | tag | description | their_handling | evidence | verdict | pm_native_reframe |
|---|---|---|---|---|---|---|---|
| RS-01 | career-ops | TR | LLM invents metrics in the tailored CV (the #1 fabrication risk of tailoring) | Post-generation fact gate: regex-extract metric-like claims (%, currency, Nx, count+noun) from the generated CV; every claim must exist in source files (cv.md, article-digest.md) or a config allowlist of "verified exceptions"; invented claims fail with the list printed; `forbidden_phrases` also enforced | verify-cv-facts.mjs:1-8, 87-103, 128-160 | adopt | The single most transplantable safety mechanism in either repo: deterministic anti-hallucination gate between LLM tailoring and anything user-facing |
| RS-02 | career-ops | TR | Overzealous markup stripping deleted real evidence from the fact allowlist: bare `<` in prose (`p<0.001`, `<30 min`) swallowed text up to the next `>`, deleting 1,327 chars incl. two metrics — failing *truthful* CVs | Tag-shaped-only stripping (`<name …>` / `</name>`), with the regression documented in a comment | verify-cv-facts.mjs:64-73 | adopt | Also a meta-lesson: the verifier itself needs regression tests against real user docs |
| RS-03 | career-ops | TR | User metrics leaking into auto-updated system prompt files get overwritten on update (and shipped to other users' evaluations) | `cv-sync-check.mjs` scans system prompt files for hardcoded metric patterns; also detects profile.yml still containing example data ("Jane Smith") | cv-sync-check.mjs:1-11, 40-60 | adapt | Hosted equivalent: lint that no user PII/metrics are cached in shared prompt templates or global config |
| RS-04 | career-ops | UX | Tailored CV variants drift from the master resume over time | Master-facts model: cv.md is canonical, variants are regenerated projections; sync check + fact gate keep variants ⊆ master | cv-sync-check.mjs:4-11; AGENTS.md (CV Source of Truth) | adopt | Core product decision: one master profile, JD-tailored variants always derived, never independently edited into drift |
| RS-05 | career-ops | TR | "Senior PM" vs "Principal PM" at the same company treated as the same opening (or sibling team roles wrongly merged) | Fuzzy role matching with three token classes: stopwords (seniority/mode/locations/repost-annotations) never count; baseline job words can support but never solely justify a match; explicit rule: if both titles specify seniority, they must share one (Senior≠Principal); match needs ≥2 shared tokens, ≥1 non-baseline, Jaccard ≥0.6 — "preserves genuine reposts while keeping sibling roles separate" | role-matcher.mjs:10-51, 60-110 | adopt | Tune token sets for PM titles (growth/platform/core/monetization are the discriminating tokens; "product"/"manager" are baseline) |
| RS-06 | career-ops | TR | Two-letter buckets (AI/ML) as similarity signal cause cross-domain false matches | Short-acronym whitelist is curated; "broad two-letter buckets such as AI/ML are intentionally excluded because they appear across many unrelated roles" | role-matcher.mjs:45-49 | adopt | — |
| RS-07 | career-ops | PC | JD-to-CV skill comparison invents gaps from noisy extraction | Zero-LLM three-bucket classifier: `existing` (named in Skills) / `supportedByResume` (in prose) / `gap` (no trace); deliberately conservative extraction — "under-extracting is recoverable by the user; over-extracting noise misreports gaps that aren't real"; nothing is ever auto-added to cv.md | jd-skill-gap.mjs:4-27, 40-52 | adopt | The `supportedByResume` middle bucket is the tailoring engine: skills the resume *supports but doesn't surface* are exactly what a JD-tailored variant should promote |
| RS-08 | career-ops | PC | Behavioral/ATS questions answered from scratch each time; stories drift from the evidence base | Reusable STAR+R story bank (markdown, user-owned) + zero-LLM matcher: weighted lexical scoring (explicit "best for" tags ×3, title/theme ×2, action+result ×1, JD-overlap boost), output trimmed to ATS paste length (250-500 words) | match-star.mjs:4-15, 118-140 | adopt | Leadership-artifact bank is the PM-persona equivalent (launch stories, conflict, prioritization); tags + retrieval, LLM only for final adaptation |
| RS-09 | career-ops | TR | ATS parsers choke on Unicode: em-dashes, smart quotes, zero-width chars, NBSP cause mojibake and parse failures (their issue #1) | Body-text-only normalization pass before PDF render (preserves CSS/JS/URLs), counts every replacement type for logging; arrows become words ("→" → " to ") since PDF text extractors strip them | generate-pdf.mjs:42-95 | adopt | — |
| RS-10 | career-ops | TR | Rendered CV silently drops or reorders sections vs the source | Post-render checks: rendered section order extracted from HTML and compared to cv.md's order (divergence fails generation); page count checked after Chromium writes the PDF — overflow warns with trimming guidance, `--strict-pages` makes it a hard reject | generate-pdf.mjs:14-23, 148-159 | adopt | — |
| RS-11 | hiring-agent | TR | Composite score computed from unvalidated category parts | Weighted composite only after schema validation + caps; category maxes live in one place; totals recomputed from parts (not trusted from LLM) | score.py:41-72; transform.py:688-702 | adopt | — |

## Domain: analytics & decision support

| case_id | repo | tag | description | their_handling | evidence | verdict | pm_native_reframe |
|---|---|---|---|---|---|---|---|
| AN-01 | career-ops | TR | Stats aggregator crashes or emits partial shapes when a data file doesn't exist yet (fresh install) | Sections degrade to `null` while the JSON contract shape stays complete; `metadata.sources` reports which files were found | stats.mjs:12-18 | adopt | Empty-state analytics is a launch-day UX requirement, not an edge case |
| AN-02 | career-ops | UX | One "active applications" number means different things to different views; averages diluted by non-pursued rows | Two deliberate, documented status sets: ACTIVE (in-flight: Applied..Offer; Evaluated is "a candidate, not an application in flight"; Hired is terminal) vs PURSUED (feeds avgScoreApplied — "plain avgScore mixes in SKIP/Discarded and understates real fit") | stats.mjs:38-48 | adopt | — |
| AN-03 | career-ops | TR | Rows with unrecognized statuses silently vanish from analytics | Unknown statuses map to a counted `Unknown` bucket — "counted, never dropped" | stats.mjs:53-58 | adopt | — |
| AN-04 | career-ops | UX | Funnel metrics lie to the user: completed-only medians are survivorship-biased (61% ghosting), small samples support fake "2x better than market" claims | Codified "statistical honesty rules (council-reviewed, non-negotiable)": medians must report right-censoring ("n still waiting, excluded"); 0-day catch-up hops excluded from medians but counted; no comparative multiplier claims below n=20; above-benchmark results always carry a selection-bias note; every benchmark cites its year and is labeled "directional" | funnel-velocity.mjs:26-33 | adopt | For a decision-support product aimed at senior PMs this is table stakes — they will catch dishonest stats instantly |
| AN-05 | career-ops | TR | Editing status history in place destroys velocity analytics | Append-only transition ledger (`status-log.tsv`) written by the canonical status writer; `to: "-"` retracts an observation, `source: correction` supersedes a same-key row — history is corrected by appending, never rewritten | funnel-velocity.mjs:14-21 | adopt | — |
| AN-06 | career-ops | TR | Salary data points contradict each other (JD range vs recruiter verbal vs offer letter vs user's ask) | Append-only observation log; fold per (application, type) by trust tier (contract > offer-letter > recruiter-verbal > user), then latest date; separate `stated` log records the number the candidate verbally committed to a *specific interviewer in a specific round* so a later round doesn't contradict it — excluded from gap math | salary-gap.mjs:4-22 | adopt | The `stated` log is negotiation-support gold for senior-comp conversations |
| AN-07 | career-ops | PC | Per-ATS-vendor outcome differences invisible (same resume advancing at different rates through different ATS pipelines) | Pattern analyzer computes per-vendor advance rates with a minimum sample floor (`--min-vendor-n 8`); explicitly motivated by algorithmic-monoculture research (Bommasani et al., FAccT 2026); machine-summary fields parsed against an allowlist | analyze-patterns.mjs:9-14, 28-50; AGENTS.md (analyze-patterns row) | adopt | — |
| AN-08 | career-ops | UX | Process friction (chaotic scheduling, poor communication) conflated with company fit or interviewer behavior | Third independent axis: candidate-authored `[process-friction: reason]` tags aggregated per company; explicitly "never tied to a named individual recruiter" | process-quality.mjs:2-17 | adopt | The privacy rule matters doubly in a hosted multi-user product |

## Domain: outreach & reply handling — FLAGGED: not committed product scope

| case_id | repo | tag | description | their_handling | evidence | verdict | pm_native_reframe |
|---|---|---|---|---|---|---|---|
| OU-01 | career-ops | UX | Follow-up nagging without limits turns the tool into a spam machine | Cadence config with a hard cap (`applied_max_followups: 2`), per-state timing defaults, all user-overridable in profile | followup-cadence.mjs:36-50 | adopt (if scope lands) | — |
| OU-02 | career-ops | PC | Follow-up feature "born dead": marking Applied never seeded the follow-up schedule, so most applications silently never got a next-follow-up date — the whole cadence feature was defeated | Seeding hook (#1430) closes the loop; seed writes a PIN DIRECTIVE, never a table row, "because a table row means a follow-up was SENT, which seeding must not claim"; last pin wins so re-seeding is safe; apply-date resolution explicitly never falls back to the tracker's date column ("usually the evaluation date, not the date the application went out") | followup-seed.mjs:3-27 | adopt (if scope lands) | The generic lesson transfers now: every feature that depends on a state transition must be wired to the transition event, and semantic distinctions (scheduled ≠ sent) must be encoded in the data model |
| OU-03 | career-ops | TR | Recruiter reply emails matched to the wrong application | Deterministic layered matching: sender-domain extraction, case/spacing-insensitive company compare, CJK company-suffix normalization (有限公司 etc.), role partial-match on significant tokens ("PY01_python开发工程师" vs "python开发工程师") | reply-matcher.mjs:5-62 | adapt (if scope lands) | Keep layered-deterministic-first design; add LLM fallback only for the residue |
| OU-04 | career-ops | UX | Interview invites often name only the company ("Schedule Your Phone Screen") and the user has several applications there | Extract company + date + req-ID-looking token; fuzzy-match and RANK candidates; "a silent wrong guess is worse than showing a short ranked list, so ambiguous input always returns all plausible candidates" (#1495) | invite-match.mjs:3-16 | adopt (if scope lands) | — |
| OU-05 | career-ops | TR | Email ingestion coupled to classification and tracker writes — one bad parse corrupts state | `paste-reply.mjs` only normalizes input into an append-only candidates file; it "never classifies or touches the tracker itself"; classification and status updates are separate, reviewable steps | AGENTS.md (paste-reply.mjs row, Main Files table) | adopt (if scope lands) | Ingest → stage → classify → human-approved apply, each step inspectable |
| OU-06 | career-ops | PC | User thinks of actions ("draft a follow-up for #7") when not in a session; intents get lost | Durable append-only agent inbox (markdown checklist) drained at session start; nothing auto-submits; items needing user input are asked about, not executed | agent-inbox.mjs:2-27 | adapt (if scope lands) | In a hosted app this is a task queue with human-approval gates — the HITL rule ("queued items are intents, not commands") is the part to keep |
