name: Janu Job Copilot FL-099 Exact3 Recovery — Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed — superseded recovery path
        shell: bash
        run: |
          echo 'This mutating FL-099 recovery workflow is retired.' >&2
          echo 'Reason: CI identity can return a successful command exit while privileged P0 tests fail from missing DocumentApp/ScriptApp authorization.' >&2
          echo 'Use FL-101 owner-validation recovery: CI may hash-verify deployment, but privileged P0 regression and canary authorization must execute under the owner phase1HealthTick context and require returned pass=true.' >&2
          exit 23
