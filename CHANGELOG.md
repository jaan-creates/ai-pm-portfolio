# Curriculum Changelog
# Curriculum Changelog

> Tracks updates to the curriculum modules as the AI landscape evolves. See `09_Curriculum_Currency_and_Refresh.md` in the project for the governing policy.

## 2026-05-13 — Session 2 close amendments (three must-fix project file edits)

- **File 08:** Added Part 5 rule 9 ("Single commit surface") to anti-fabrication doctrine — commits originate from local editor only; GitHub web UI for emergencies only, logged same-day. Added matching bullet to Part 6 "Allowed in memory" list. Source: Session 2 git divergence resolution (90 min spent reconciling parallel edits from VS Code + GitHub web UI + Cursor).
- **File 10:** Added mandatory verification ritual to `SAVE STATE` description (run `git status` + `git log --oneline -3` and paste outputs before generating artifacts). Added `Verified:` line to CHECKPOINT block format. Source: Session 2 recurrence of "claim before verify" pattern within 24 hours of first surfacing.
- **File 02:** Added "Platform-specific commands" section. Establishes that mentor must default to OS-appropriate shell flavoring (PowerShell for Windows learners) and names specific PowerShell footguns (`Out-File` encoding, `fc` alias collision, `where` alias). Source: Session 2 mentor calibration notes #2 and #3.

All three amendments are additive (no existing content removed). The architecture from files 08, 10, and 02 is preserved; these edits make implicit assumptions explicit and add forcing functions that Session 2 proved necessary.

## 2026-05-12 — Baseline
...


- Phase 0 (Builder's Toolkit) baseline established.
- All other phases have a `Next due` date in PROGRESS.md, refreshed on entry.
- Next monthly currency check (`WHAT'S NEW`): ~2026-06-12.
- Next quarterly refresh (`REFRESH CURRICULUM`): ~2026-08-12.

# Curriculum Changelog

> Tracks updates to the curriculum modules as the AI landscape evolves. See `09_Curriculum_Currency_and_Refresh.md` in the project for the governing policy.

